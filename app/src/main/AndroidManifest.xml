<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 已有權限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 前台服務必需權限（Android 8.0+） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- 新增：用於 specialUse 類型的權限（Android 14+ 必需） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:resizeableActivity="true"
            android:theme="@style/TransparentTheme"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".FloatingViewService"
            android:exported="false"
            android:foregroundServiceType="specialUse">

            <!-- 添加使用理由（必须） -->
            <property
                android:name="android.foreground_service_type_special_use_description"
                android:value="@string/foreground_service_special_use_description" />
        </service>
    </application>
</manifest>